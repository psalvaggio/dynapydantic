[project]
name = "shape-plugins"
version = "0.1.0"
dependencies = ["base-package", "dynapydantic"]

[project.entry-points."shape.plugins"]
# It can be either of these
shape-plugins = "shape_plugins.registration:register_models"
# shape-plugins = "shape_plugins.registration"
